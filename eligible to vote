#include<iostream>
using namespace std;
int main()
{
	int a;
	cout<<" enter the age :";
	cin>>a;
	if(a>=18)
	   cout<<"  you are eligible to vote ";
	else if(a<0)
	    cout<<" enter valid age ";
	else
	    cout<<(18-a)<<" required to vote ";
	return 0;

}
